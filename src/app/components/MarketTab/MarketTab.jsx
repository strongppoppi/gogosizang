import StoreRanking from "./StoreRanking";

export default function MarketTab({ marketIndex }) {
    return (
        <div className="w-full flex flex-col items-center">
            <StoreRanking />
        </div>

    )
}